function setupWin() {{
  // Setup the Window

  const psychoJS = new PsychoJS({{ debug: true}});
  psychoJS.createWindow({{
      psychoJS: psychoJS,
      fullscr: {fullscr},
      screen: {params[Screen]},
      color: new Color({params[color]}),
      units: {params[Units]}
      }});
}}

